/**
 * Re-exported shared types for convenience.
 *
 * Types that are used across multiple commands should be re-exported here
 * so consumers can import from `../shared/types.js` rather than reaching
 * into specific module internals.
 */
export type { SkillSection } from "./sections.js";
